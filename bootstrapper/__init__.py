"""bootstrapper-og.

An auditable retrieval-evaluation harness for ANN search over documents, paired with a
native-Streamlit education layer. The engine in :mod:`bootstrapper.core` is dataset-agnostic;
finance-specific loading lives behind a ``DatasetAdapter`` in :mod:`bootstrapper.datasets`.
"""

__version__ = "0.1.0"
